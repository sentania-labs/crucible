# Placeholder. `kubeseal` against the cluster's own key produces the real encryptedData;
# the result lives in lab-admin's GitOps repository, not here. `password` and `url` are
# two views of one value and must agree (see ../README.md).
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: crucible-database
  namespace: crucible
spec:
  encryptedData:
    password: REPLACE_WITH_SEALED_DATABASE_PASSWORD
    url: REPLACE_WITH_SEALED_DATABASE_URL
  template:
    metadata:
      name: crucible-database
      namespace: crucible
    type: Opaque
